const LoginHeader = () => (
    <header className="absolute inset-x-0 top-0 pt-6">
        <div className="container">
            <span className="select-none font-headings text-xl font-black tracking-widest">
                SENDA
            </span>
        </div>
    </header>
);

export default LoginHeader;
