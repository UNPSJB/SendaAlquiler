query dashboard {
    allSales {
        id
        createdOn
        total
        client {
            firstName
            lastName
        }
    }
    # allClients {
    #     id
    # }
    allProducts {
        id
        name
        saleItems {
            quantity
        }
    }
}
